[tool.poetry]
name = "big-data-20251"
version = "0.1.0"
description = "Crawl legal documents (thuvienphapluat) used by the Big Data course"
authors = ["you <you@example.com>"]
license = "MIT"

[tool.poetry.dependencies]
# Allow Python 3.11 and 3.12 to match common local environments
python = ">=3.11,<4.0"
selenium = "^4.31.0"
webdriver-manager = "^4.0.0"
beautifulsoup4 = "^4.13.0"
requests = "^2.32.0"

[build-system]
requires = ["poetry-core>=1.5.0"]
build-backend = "poetry.core.masonry.api"
